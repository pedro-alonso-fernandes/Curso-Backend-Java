package oo.heranca;

public enum Direcao {

	// O "Enum" serve para padronizar valores constantes, ou seja, valores em que já se sabe
	// sua quantidade exata e por isso não vão mudar (como por exemplo: dias da semana, meses
	// do ano, etc).
	
	// Nesse caso, usei para definir a direção dos movimentos de um jogo 2D.
	
	NORTE, LESTE, SUL, OESTE;
	
}
